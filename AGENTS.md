# Agent guide

Shuvscan is an evidence-first Linux security scanner. The pre-commit checklist and the
design constraints every change must preserve live in the README `Development` section;
probe acceptance rules live in `SECURITY.md`. Follow both.

Use Jujutsu (`jj`) for local version-control work.
